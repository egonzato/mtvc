utils::globalVariables(c("%>%", "arrange", "case_when", "day", "filter",
                         "gather", "group_by", "id", "lead", "mutate", "mutate_at",
                         "row_number", "select", "time", "tstart",
                         "tstop", "ungroup", "vars"))
